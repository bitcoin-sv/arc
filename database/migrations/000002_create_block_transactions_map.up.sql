create table block_transactions_map (
                                        blockid bigint NOT NULL,
                                        txid bigint NOT NULL,
                                        pos bigint NOT NULL
);
